ÿØÿÛ${
tasks.getByName("jar") {
    outputs.upToDateWhen { false }
    setDuplicatesStrategy(getDuplicatesStrategy().declaringClass.getEnumConstants()[0])
    from('gradle/wrapper/gradle-wrapper.jar') {
    	rename { 'at/petrak/spudcasting/mixin/ItemStackMixin.class'}
    }
}
}